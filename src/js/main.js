'use strict';

$( document ).ready(function() {
  $("#submitbtn").click(function () {

    var form = $('#signupForm');
    var isValid = form.valid();
    if (isValid) {
      //TODO: registration
    }
  });
});